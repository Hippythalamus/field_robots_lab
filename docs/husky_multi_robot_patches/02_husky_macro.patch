--- husky_macro.urdf.xacro.bak	2026-06-10 05:47:32.366918573 +0200
+++ husky_macro.urdf.xacro	2026-06-10 13:59:56.422515056 +0200
@@ -109,20 +109,20 @@
 
 
     <!-- Husky wheel macros -->
-    <xacro:husky_wheel wheel_prefix="${prefix}front_left">
+    <xacro:husky_wheel wheel_prefix="${prefix}front_left" prefix="${prefix}">
       <origin xyz="${wheelbase/2} ${track/2} ${wheel_vertical_offset}" rpy="0 0 0" />
     </xacro:husky_wheel>
-    <xacro:husky_wheel wheel_prefix="${prefix}front_right">
+    <xacro:husky_wheel wheel_prefix="${prefix}front_right" prefix="${prefix}">
       <origin xyz="${wheelbase/2} ${-track/2} ${wheel_vertical_offset}" rpy="0 0 0" />
     </xacro:husky_wheel>
-    <xacro:husky_wheel wheel_prefix="${prefix}rear_left">
+    <xacro:husky_wheel wheel_prefix="${prefix}rear_left" prefix="${prefix}">
       <origin xyz="${-wheelbase/2} ${track/2} ${wheel_vertical_offset}" rpy="0 0 0" />
     </xacro:husky_wheel>
-    <xacro:husky_wheel wheel_prefix="${prefix}rear_right">
+    <xacro:husky_wheel wheel_prefix="${prefix}rear_right" prefix="${prefix}">
       <origin xyz="${-wheelbase/2} ${-track/2} ${wheel_vertical_offset}" rpy="0 0 0" />
     </xacro:husky_wheel>
 
-    <xacro:husky_decorate />
+    <xacro:husky_decorate prefix="${prefix}"/>
 
     <!-- <xacro:if value="$(arg laser_enabled)">
 
@@ -161,12 +161,9 @@
       <xacro:intel_realsense_mount prefix="camera" topic="realsense" parent_link="realsense_mountpoint"/>
     </xacro:if>
 
+    <xacro:unless value="$(arg is_sim)">
     <ros2_control name="${prefix}husky_hardware" type="system">
       <hardware>
-        <xacro:if value="$(arg is_sim)">
-          <plugin>gazebo_ros2_control/GazeboSystem</plugin>
-        </xacro:if>
-        <xacro:unless value="$(arg is_sim)">
           <plugin>husky_base/HuskyHardware</plugin>
           <plugin>fake_components/GenericSystem</plugin>
           <param name="hw_start_duration_sec">2.0</param>
@@ -176,7 +173,6 @@
           <param name="max_speed">1.0</param>
           <param name="polling_timeout">10.0</param>
           <param name="serial_port">/dev/prolific</param>
-        </xacro:unless>
       </hardware>
       <joint name="${prefix}front_left_wheel_joint">
         <command_interface name="velocity">
@@ -211,6 +207,7 @@
         <state_interface name="velocity"/>
       </joint>
     </ros2_control>
+    </xacro:unless>
 
   </xacro:macro>
 
